# Only needed to run scripts/train.py locally with real data/network —
# deliberately kept out of requirements.txt so the importable app/ package
# (dataset merging, promotion logic, registry) stays lightweight.
-r requirements.txt
ultralytics==8.3.28
mlflow==2.17.2
requests==2.32.3
# ultralytics==8.3.28 calls the removed numpy.trapz internally (renamed to
# numpy.trapezoid in NumPy 2.0); pip resolves numpy unpinned otherwise,
# which grabs latest 2.x and crashes mid-validation. Cap below 2.0 to match
# what 8.3.28 was actually built against.
numpy<2.0
